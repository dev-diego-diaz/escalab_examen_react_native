import React from "react";
import { View, Text } from "react-native";

const UserScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 35 }}> Perfil de usuario </Text>
    </View>
  );
};

export default UserScreen;
