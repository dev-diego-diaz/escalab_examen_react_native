import React from "react";
import { View, Text } from "react-native";

const ActorsScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 35 }}> Actores </Text>
    </View>
  );
};

export default ActorsScreen;
